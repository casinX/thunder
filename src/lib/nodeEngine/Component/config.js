export const DEFAULT_TAG_NAME = 'div';

export const DEFAULT_FPS = 60;
export const DEFAULT_SENSITIVE_STATE = false;
export const DEFAULT_NO_CACHE_STATE = false;

export const SERVICE_PROPS = [
    'mode',
    'key',
];

export const TEXT_ELEMENT_TYPE = Symbol('text-element');
export const TAG_ELEMENT_TYPE = Symbol('tag-element');
export const COMPONENT_ELEMENT_TYPE = Symbol('component-element');


export default {
    DEFAULT_TAG_NAME,
    DEFAULT_FPS,
    DEFAULT_SENSITIVE_STATE,
    DEFAULT_NO_CACHE_STATE,
    SERVICE_PROPS,
    TEXT_ELEMENT_TYPE,
    TAG_ELEMENT_TYPE,
    COMPONENT_ELEMENT_TYPE,
}